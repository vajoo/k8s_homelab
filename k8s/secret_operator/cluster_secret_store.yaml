apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://192.168.178.92:8200"
      path: "kv/org"
      version: "v2"
      auth:
        tokenSecretRef:
          name: "vault-token"
          namespace: "demo"
          key: "token"